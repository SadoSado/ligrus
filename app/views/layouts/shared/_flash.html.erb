<% if !flash.empty? %>
  <div class="relative">
    <div
      data-controller="notification"
      data-notification-delay-value="2000"
      class="absolute m-auto left-0 right-0 transition duration-1000 hidden bg-blue-100 rounded-lg py-5 px-6 text-base text-blue-700 mb-3 w-max"
      data-transition-enter-from="opacity-0 translate-x-6"
      data-transition-enter-to="opacity-100 translate-x-0"
      data-transition-leave-from="opacity-100 translate-x-0"
      data-transition-leave-to="opacity-0 translate-x-6"
    >
        <% flash.keys.each do |k| %> 
                <%= flash[k] %>
              <% end %>
    <button data-action="notification#hide">Close</button>
      </div>
    </div>
<% end %>
